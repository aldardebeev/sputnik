# Используйте базовый образ nginx:alpine
FROM nginx:alpine
ADD ./conf.d/nginx.conf /etc/nginx/conf.d/default.conf

# Добавьте настройки или инструкции для вашего контейнера
# ...

# Опционально: скопируйте свои файлы конфигурации или статические файлы в контейнер
# COPY ./каталог_локальный /каталог_контейнера

# Опционально: определите порт, который контейнер будет прослушивать
# EXPOSE порт

# Опционально: выполните любые дополнительные команды или инструкции
# ...

# Опционально: укажите команду, которая будет выполнена при запуске контейнера
# CMD [команда]